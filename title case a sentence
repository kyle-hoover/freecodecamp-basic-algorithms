//we want to split this into an array based on each word
//then we probably want to map through each part of the array and use .uppercase
//we also need to lowercase everything else
//then we can join it back together as string

function titleCase(str) {
  var spl = str.split(" ");
  
//var spl will separate our string into an array with each word as an input

  
